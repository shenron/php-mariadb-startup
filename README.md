# PHP project with MariaDB + Nginx

## start
> ./start.sh


## phpmyadmin
reachable on http://localhost:8080

## website
reachable on http://localhost:8282

## Install
To install environment in an existing project;
> curl -s https://raw.githubusercontent.com/shenron/php-mariadb-startup/master/install.sh | bash -s
